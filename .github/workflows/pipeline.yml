name: Examen Calidad

on: [push]

jobs:
  calidad:
    runs-on: ubuntu-latest

    steps:
    - name: Ejecutar tests
      run: mvn test

    - name: Checkout
      uses: actions/checkout@v4

    - name: Setup Java
      uses: actions/setup-java@v4
      with:
        java-version: 17



